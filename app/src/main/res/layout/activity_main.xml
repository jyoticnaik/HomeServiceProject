<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity"
        android:id="@+id/rlay"
        android:background="#272541"
        tools:ignore="ContentDescription">

    <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" app:srcCompat="@drawable/ellipse"
            android:id="@+id/circlesym" android:layout_marginTop="208dp"/>

    <TextView
            android:text="@string/HS"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/Logosym"
            android:textSize="70sp"
    />
    <TextView
            android:text="@string/HomeService"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/logosubsym" android:layout_marginTop="80dp"
    />

</RelativeLayout>